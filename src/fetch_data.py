"""DEPRECATED — was used for yfinance fallback before the 1m tick data was provided.
The final paper uses the 1-minute tick data in data/exported_1m_csv/ directly."""
raise RuntimeError("fetch_data.py is deprecated. See src/analysis.py.")
