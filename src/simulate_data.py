"""DEPRECATED — was used to produce synthetic fallback data during early
development before the real 1-minute tick data was available.
The final paper uses real data only. See src/analysis.py."""
raise RuntimeError("simulate_data.py is deprecated.")
